/**
 * This file was automatically generated by templates/formats.hbs.
 * DO NOT MODIFY IT BY HAND. Instead, modify templates/formats.hbs,
 * and run build script to regenerate this file.
 */

use serde_json::Value;
use serde::{Serialize, Deserialize};

{{#each formats}}
schemafy::schemafy!(
  root: PolywrapManifest{{fsuffix format}}
  "schemas/{{format}}.json"
);

{{/each}}

#[derive(Clone)]
pub enum AnyManifest {
  {{#each formats}}
  PolywrapManifest{{fsuffix format}}(PolywrapManifest{{fsuffix format}}),
  {{/each}}
}

impl AnyManifest {
  pub fn format(&self) -> String {
    match self {
      {{#each formats}}
      AnyManifest::PolywrapManifest{{fsuffix format}}(_) => "{{format}}".to_string(),
      {{/each}}
    }
  }

  pub fn get_latest(&self) -> Result<PolywrapManifest, polywrap_core::error::Error> {
        match self {
            AnyManifest::PolywrapManifest{{fsuffix latest_format}}(m) => Ok(m.clone()),
            _ => Err(polywrap_core::error::Error::ManifestError(
                "Invalid manifest format".to_string(),
            )),
        }
    }

    pub fn from_json_value(value: Value) -> Self {
        match value["format"].as_str().unwrap() {
            {{#each formats}}
            "{{format}}" => AnyManifest::PolywrapManifest{{fsuffix format}}(serde_json::from_value(value).unwrap()),
            {{/each}}
            _ => panic!("Invalid manifest format"),
        }
    }

    pub fn to_json_value(&self) -> Value {
        match self {
            {{#each formats}}
            AnyManifest::PolywrapManifest{{fsuffix format}}(manifest) => serde_json::to_value(manifest).unwrap(),
            {{/each}}
        }
    }
}

pub const LATEST_MANIFEST_FORMAT: &str = "{{latest_format}}";
pub type PolywrapManifest = PolywrapManifest{{fsuffix latest_format}};